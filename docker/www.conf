; Draplo PHP-FPM pool overrides

[www]
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8
pm.max_requests = 1000

; Slow log for debugging
slowlog = /var/log/php-fpm-slow.log
request_slowlog_timeout = 10s
